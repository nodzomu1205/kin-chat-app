# Kin Chat App Handoff

Last updated: 2026-04-26

## Session Summary

This session focused on library / ingest product regressions and then reviewed
the surrounding authority chain for stale residue.

Current verification state at closeout:

- `npx tsc --noEmit` passes
- `npm run lint` passes
- `npm test` passes
- `npm run build` passes
- current test status: `151 files / 659 tests`

Next-session default:

- treat the repository as late-stage maintenance-watch, roughly `85-90%`
  complete
- do not continue splitting send-to-GPT by default; it is now a regrowth-watch
  boundary
- start with `hooks/useGoogleDrivePicker.ts` if no higher-priority product bug
  appears
- before editing Drive picker code, check unused Drive/import residue and keep
  Drive HTTP operations in `lib/app/google-drive/googleDriveApi.ts` while
  importability/summary/stored-text shaping remains in
  `googleDrivePickerBuilders.ts`

Latest maintenance update:

- removed the dead GPT settings/persistence `responseMode` carry-through:
  `usePersistedGptOptions` no longer exposes a no-op response-mode setter,
  `persistedGptOptionsState` no longer stores response-mode state, and
  page/workspace/panel settings builders no longer pass response-mode props
- active request/task behavior is intentionally unchanged; the controller
  service boundary now supplies the current fixed `strict` runtime value as
  `reasoningMode`, with `lib/app/task-runtime/reasoningMode.ts` owning the runtime
  strict/creative type
- task-intent fallback, current-task intent refresh, and Kin task start now also
  use `reasoningMode` at their flow boundaries
- Kin transfer, transform-intent, and send-to-GPT request internals now also use
  `reasoningMode`
- remaining `responseMode` references are protocol/task payload semantics
  (`STRUCTURED_RESULT`, `SILENT_ACK`, `FINAL_ACK`, `TaskResponseMode`), not
  removed UI policy or LLM strict/creative runtime state
- file and Drive imports now share generated-summary resolution and ingest usage
  aggregation through `lib/app/ingest/importSummaryGeneration.ts`
- file and Drive imports now share stored ingested-document record construction
  through `buildIngestedDocumentRecord`
- app-side ingest modules were moved under `lib/app/ingest/` as the first
  folder-organization pass
- send-to-GPT app-side modules were moved under `lib/app/send-to-gpt/` as the
  second folder-organization pass
- memory-interpreter app-side modules were moved under
  `lib/app/memory-interpreter/` as the third folder-organization pass
- GPT-memory app-side modules were moved under `lib/app/gpt-memory/` as the
  fourth folder-organization pass
- task-draft app-side modules were moved under `lib/app/task-draft/` as the
  fifth folder-organization pass
- task-runtime app-side modules were moved under `lib/app/task-runtime/` as the
  sixth folder-organization pass
- Kin protocol core and sidecar modules were moved under `lib/app/kin-protocol/`
  as the seventh folder-organization pass
- reference-library app-side modules were moved under
  `lib/app/reference-library/` as the eighth folder-organization pass
- search-history app-side modules were moved under `lib/app/search-history/`
  as the ninth folder-organization pass
- panel/UI state app-side modules were moved under `lib/app/ui-state/` as the
  tenth folder-organization pass
- remaining app-side utility clusters were moved under dedicated folders:
  `gpt-task/`, `youtube-transcript/`, `memory-rules/`, `multipart/`,
  `task-support/`, `google-drive/`, `auto-bridge/`, and `gpt-context/`
- task-domain modules were moved under `lib/task/`, starting the `lib` root
  organization pass
- memory-domain modules were moved under `lib/memory-domain/`
- shared primitives were moved under `lib/shared/`, and the search facade was
  moved to `lib/search-domain/search.ts`
- component root files were moved under `components/layout/`,
  `components/message/`, `components/ui/`, and `components/pwa/`
- post-organization repository review was recorded in
  `docs/repository-review-2026-04-25.md`
- `components/panels/gpt/ReceivedDocsDrawer.tsx` was renamed to
  `LibraryDrawer.tsx` and split into drawer state, import/tab controls, library
  item shell, item header/actions/metadata, search preview, and
  stored-document editing, while keeping action callbacks on the existing
  reference/settings boundaries
- the unused `components/panels/gpt/SearchRawDrawer.tsx` wrapper was removed
  after a repo-wide reference check found no live imports
- `components/panels/gpt/LibraryDrawer.test.tsx` is now included in the
  Vitest baseline instead of sitting as an excluded component test
- `components/panels/gpt/GptDrawerRouter.tsx` now delegates device-import
  option shaping, meta/task/library/settings drawer prop bundles, and
  memory-settings reset/save value shaping to `GptDrawerRouterHelpers.ts`, with
  focused helper coverage in the Vitest baseline
- `components/panels/gpt/GptSettingsDrawer.tsx` and
  `components/panels/gpt/GptSettingsWorkspace.tsx` now share search preset,
  engine-toggle, and source-count normalization through
  `GptSettingsSearchState.ts`, with focused helper coverage in the Vitest
  baseline
- `components/panels/gpt/GptSettingsDrawer.tsx` now delegates memory and ingest
  tab bodies to `GptSettingsDrawerSections.tsx`
- `components/panels/gpt/GptSettingsWorkspaceViews.tsx` now reuses the shared
  memory settings section and memory reset/save builders
- the unused `components/panels/gpt/GptSettingsWorkspaceSections.tsx` re-export
  layer and orphaned generic `RuleApprovalSection` were removed
- `components/panels/gpt/GptSettingsApprovalSections.tsx` and
  `components/panels/gpt/GptSettingsRulesSection.tsx` now share memory review
  options and topic-decision value shaping through `GptSettingsApprovalState.ts`
- `components/panels/gpt/GptSettingsApprovalSections.tsx` now delegates
  memory/sys approval item cards to `GptSettingsApprovalCards.tsx`
- `components/panels/gpt/GptSettingsRulesSection.tsx` now delegates
  pending/approved memory and SYS rule cards to `GptSettingsRuleCards.tsx`
- `components/panels/gpt/TaskProgressPanel.tsx` now delegates
  `SYS_TASK_CONFIRM` output construction to `TaskProgressPanelHelpers.ts`
- `components/panels/gpt/TaskProgressPanel.tsx` now delegates
  requirement/request cards and sync/suspend action sections to
  `TaskProgressPanelParts.tsx`
- `lib/search-domain/normalizerBuilders.ts` now delegates AI Mode block/table
  shaping to `normalizerAiModeBuilders.ts`, local-result shaping to
  `normalizerLocalBuilders.ts`, YouTube shaping to
  `normalizerYoutubeBuilders.ts`, and shared raw-text block formatting to
  `normalizerRawBlocks.ts`
- `lib/app/send-to-gpt/sendToGptFlowGuards.ts` now delegates pure gate-context
  builders to `sendToGptFlowGateContextBuilders.ts`
- `app/page.tsx` now delegates the root viewport shell and panel layout wrapper
  to `components/layout/ChatAppShell.tsx`
- chat-page defaults now live in `lib/app/ui-state/chatPageDefaults.ts`
- `app/page.tsx` now keeps chat UI state, GPT options, token usage, bridge
  settings, Kin manager state, task-draft workspace state, search history,
  task/protocol state, and reference-library state as named domain bundles
  before workspace composition
- `hooks/useChatPageWorkspaceDomainInputs.ts` now maps those page-domain bundles
  into the workspace state/action/service contract, reducing `app/page.tsx` to
  page orchestration plus shell handoff
- `hooks/chatPagePanelCompositionBuilders.ts` now delegates section-level panel
  arg shaping to `chatPagePanelSectionBuilders.ts` and task snapshot persistence
  to `chatPageTaskSnapshot.ts`
- `hooks/chatPagePanelCompositionTypes.ts` now re-exports split panel and
  workspace-view type modules from `chatPagePanelArgsTypes.ts` and
  `chatPageWorkspaceViewTypes.ts`
- `hooks/chatPageWorkspaceReferenceTypes.ts` now owns the reference-library /
  Drive workspace view contract that was previously embedded in
  `chatPageWorkspaceViewTypes.ts`
- `lib/app/task-runtime/transformIntentResolver.ts` now owns API intent
  resolving, JSON extraction, and LLM patch merge/coercion, leaving
  `transformIntentParser.ts` focused on rule-based directive parsing
- `lib/app/task-runtime/kinTransferFlowTypes.ts` now owns Kin transfer flow
  argument/result contracts, reducing type noise in `kinTransferFlows.ts`
- `lib/app/task-runtime/kinTransferFlows.ts` now stays as the public transfer
  facade while `kinTransferLatestFlow.ts` and
  `kinTransferCurrentTaskFlow.ts` own the two orchestration paths
- `lib/app/send-to-gpt/sendToGptPreparedRequestGates.ts` now owns
  prepared-request gates, leaving `sendToGptFlowGuards.ts` focused on
  pre-preparation gates plus public re-exports
- `lib/app/send-to-gpt/sendToGptApiTypes.ts` now owns ChatGPT API
  request/response/search-source contracts, reducing the breadth of
  `sendToGptFlowTypes.ts`
- `lib/app/send-to-gpt/sendToGptPreparedRequestTypes.ts` now owns
  prepared-request and gate context contracts, further narrowing
  `sendToGptFlowTypes.ts`
- `lib/app/send-to-gpt/sendToGptFlowArgTypes.ts`,
  `sendToGptFlowArtifactTypes.ts`, and `sendToGptFlowBaseTypes.ts` now own the
  former flow type groups directly, so the unused `sendToGptFlowTypes.ts`
  facade was removed
- `lib/app/send-to-gpt/sendToGptFlowContextResolvers.ts` now owns
  inline-search extraction, AI continuation artifacts, protocol limit priority,
  and protocol search-engine overrides, keeping `sendToGptFlowContext.ts`
  closer to public context composition
- `lib/app/send-to-gpt/sendToGptPreparedGateHandlers.ts` now owns
  prepared-request gate side effects, leaving `sendToGptPreparedRequestGates.ts`
  focused on gate context assembly and decision dispatch

## User-Facing Baseline Now Expected

### Library-card summary toggle

The setting now means:

- `ライブラリカードへの取込時にサマリー自動生成`
- applies across file import, Google Drive import, YouTube transcript import,
  search library saves, and task snapshot saves
- when off, library cards should store no generated summary
- when on, summary-generation usage should be counted in the ingest bucket

### File / Drive import feedback

File import and Google Drive import should now:

- show the shared loading indicator while work is running
- save into the library
- post a visible GPT chat task-info message after import completes
- include extracted character count in that completion message

### PDF / image ingest modes

The user-facing image/PDF choices are:

- `compact`
- `detailed`
- `max`

The internal legacy value for compact image detail is still `simple`. Treat this
as compatibility plumbing, not user-facing copy.

### Ingest output authority

The `/api/ingest` prompt and result selection now intentionally avoid asking
for multiple alternate versions of the same content:

- text-first PDF/image `max`: `rawText` is the authority
- `detailed`: `kinDetailed` is the authority
- `compact`: `kinCompact` is the authority

Generated `compact` and `detailed` lines should not be clipped again by a local
post-selection character budget. The previous clipping path caused final list
items to end with `...` and lose content.

## What Changed In Code

Key files:

- `lib/server/ingest/promptBuilder.ts`
- `lib/server/ingest/resultSelection.ts`
- `lib/server/ingest/routeHelpers.ts`
- `lib/app/ingest/fileIngestFlow.ts`
- `hooks/useGoogleDrivePicker.ts`
- `hooks/useGptMessageActions.ts`
- `hooks/useSearchHistory.ts`
- `hooks/chatPagePanelCompositionBuilders.ts`
- `components/panels/gpt/GptSettingsLibrarySections.tsx`
- `components/panels/gpt/GptSettingsDrawer.tsx`
- `components/panels/gpt/GptSettingsWorkspaceViews.tsx`

Important cleanup:

- removed unused intermediate-budget ingest helpers from `routeHelpers.ts`
- removed stale docs references to the deleted `hooks/useIngestActions.ts` /
  `legacyIngestHelpers` path from current docs

## Review Notes

Fresh review found no remaining live use of:

- `chooseIntermediateLines`
- `chooseDetailedLinesWithinBudget`
- `estimateIntermediateBudget`
- `chooseWholeLinesWithinBudget`
- user-facing `<option value="simple">simple`
- current docs pointing at `hooks/useIngestActions.ts`

Older dated handoffs may still mention removed paths as historical context. Do
not treat those older references as current architecture.

## Remaining Maintenance Work

Default next order after the repository-wide organization pass:

1. continue task-runtime decomposition only where focused tests preserve current
   task-constraint and Kin-transfer behavior
2. continue Drive / ingest convergence only where new duplication appears across
   `hooks/useGoogleDrivePicker.ts`, `hooks/googleDrivePickerBuilders.ts`,
   `lib/app/google-drive/googleDriveApi.ts`, and shared ingest helpers
3. continue large UI-surface splits only where the owner boundary is clear,
   especially if library drawer or message source preview behavior grows again
4. keep GPT panel/settings components from regrowing mixed UI/policy ownership
5. keep remaining protocol/task-payload `responseMode` names under
   maintenance-watch
6. keep opportunistic mojibake cleanup limited to active owner files that are
   already being touched

Read first next time:

- `docs/repository-review-2026-04-25.md`
- `docs/next-session.md`
- `docs/maintenance-checklist.md`
- `docs/refactor-roadmap.md`
- `docs/ingest-pipeline.md`
- `lib/app/ingest/ingestDocumentModel.ts`
- `lib/app/ingest/fileIngestFlow.ts`
- `hooks/useFileIngestActions.ts`
- `hooks/useGoogleDrivePicker.ts`
- `lib/server/ingest/promptBuilder.ts`
- `lib/server/ingest/resultSelection.ts`
