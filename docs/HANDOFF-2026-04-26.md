# Kin Chat App Handoff

Last updated: 2026-04-26

## Verification

Closeout verification passed:

- `npx tsc --noEmit`
- `npm run lint`
- `npm test` (`166 files / 739 tests`)
- `npm run build`

## Session Summary

This session completed the library / Kin protocol product slice and then did a
repo-wide residue check.

Implemented and verified:

- collapsible library operation area with compact bulk display and bulk Kin-send
- shared library aggregation modes: `Index`, `Summary`, `Summary + Detail`
- ordinary library-to-Kin sends use `SYS_INFO`
- task-time library reference uses only `SYS_LIBRARY_DATA_REQUEST / RESPONSE`
- retired `SYS_LIBRARY_INDEX/ITEM` and `SYS_FILE_SAVE` blocks are not active
  parser inputs
- Draft Preparation, Draft Modification, and File Saving protocol handling
- File Saving saves through the library-summary generation path
- File Saving validates active length constraints before saving
- latest full draft resolution supports `DOCUMENT_ID: Unknown`
- blank `DOCUMENT_ID` in a full modification response is rescued by mapping it
  to the previous full draft id
- Kin communication retry SYS input can auto-send again after the input was
  cleared
- memory fact extraction now filters system/noise messages and picks fairly from
  meaningful chat-visible content

## Manual Retest First

Next manual test should start with:

1. run a task that creates a draft
2. force or request `DRAFT_MODIFICATION`
3. trigger a length failure if possible
4. modify again
5. request `FILE_SAVING`
6. confirm the saved library card has a real generated summary and real
   `DOCUMENT_ID`
7. confirm `SYS_TASK_DONE` lists the saved document id

If this fails, trace in this order before patching:

1. prompt sent to GPT
2. raw GPT reply
3. parsed protocol event
4. `draftDocumentResolver.ts`
5. `sendToGptPreparedGateHandlers.ts`
6. resulting library card

## Maintenance Recommendations

Current repo status is `late-stage maintenance-watch`, not active rescue.

Recommended posture:

- do not start broad refactors without a concrete product or bug boundary
- keep `sendToGptFlow.ts` orchestration-only and watch for regrowth
- keep page/controller/panel composition in regrowth-watch
- keep ingest authority and token accounting under watch when new ingest
  behavior is added
- keep protocol/task-payload `responseMode` naming distinct from the removed
  UI/settings reasoning-mode path
- keep old library/file-saving protocol aliases retired unless a real migration
  need appears
- keep draft/file-saving logic centralized in the current resolver/gate/builder
  files; do not add local latest-draft heuristics near UI code

## Read First Next Time

- `docs/next-session.md`
- `docs/protocol-actions.md`
- `docs/maintenance-checklist.md`
- `docs/refactor-roadmap.md`
- `docs/domain-model.md`

