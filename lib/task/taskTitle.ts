export function generateTaskTitle(params: {
  goal: string;
  outputType?: string;
  entities?: string[];
}): string {
  void params.goal;
  void params.outputType;
  void params.entities;
  return "";
}
